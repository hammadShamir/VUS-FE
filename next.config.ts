/** @type {import('next').NextConfig} */
const nextConfig: import("next").NextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "storage.googleapis.com","scontent-iad3-1.cdninstagram.com"],
  },
};

export default nextConfig;
